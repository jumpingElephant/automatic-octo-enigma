
import { format } from 'automatic-octo-enigma/helpers/format';
import { module, test } from 'qunit';

module('Unit | Helper | format');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = format([42]);
  assert.ok(result);
});

